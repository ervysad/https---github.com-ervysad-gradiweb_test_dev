{% paginate collection.products by 6 %}
  {% for product in collection.products %}
    <h1>Not Products to display.</h1>
  {% else %}
    <h1>Not Products to display.</h1>
  {% endfor %}

  {% render 'pagination', pagination: paginate %}
{% endpaginate %} 